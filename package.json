{
  "name": "beezard-prototype",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@testing-library/dom": "^10.4.0",
    "@vitest/coverage-v8": "^5.0.1",
    "jsdom": "^26.1.0",
    "vite": "^7.1.5",
    "vitest": "^5.0.1"
  }
}
